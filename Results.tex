\vspace{4cm}
With the theoretical framework for gravitational lensing 
of GWs now established, including the geometric optics 
formalism, the signatures of strong lensing on GW signals, 
and the joint PE approach that exploits lensed image pairs to improve sky localization, we are in a position to present the original results of this thesis. Chapter~\ref{ch:simulation} describes the simulation setup and the strongly lensed BBH population generated with \textsc{LeR}. Chapter~\ref{Parameter_estimation_pipeline} presents the PE pipeline. Each image is first analyzed individually with \textsc{bilby}, and the two images of each event are then analyzed jointly with \textsc{hanabi}, which coordinates the coherent analysis of the image pair, sharing the intrinsic binary parameters between images while sampling the lensing parameters, on top of the \textsc{bilby} inference backend. The chapter also describes the automation infrastructure deployed on the Picasso HPC cluster. Finally, Chapter~\ref{Skyloc} presents the sky localization results, comparing the 90\% credible sky area obtained from single-image and joint analyses across the final set of analyzed events.


\newpage


\chapter{Simulation setup and implementation}
\label{ch:simulation}

This chapter describes the construction of the lensed 
BBH population used as input to the PE pipeline. The simulation proceeds in two stages: first, a large Monte Carlo (MC) population of strongly lensed BBH events is generated with \textsc{LeR} v0.4.2 
\cite{phurailatpam2026lerlvkligovirgokagracollaboration}, and then a realistic detectability filter is applied to select the subset of events that would be observed by the H1 and L1 detectors under O5 sensitivity conditions.

\section{Lensed BBH population with \textsc{LeR}}
\label{sec:LER_population}

\textsc{LeR} (LVK Event Rate calculator) is a 
Python framework for the joint simulation and rate 
computation of unlensed and strongly lensed GW 
populations \cite{phurailatpam2026lerlvkligovirgokagracollaboration}. It integrates source and lens population sampling, lens equation solving via \textsc{lenstronomy} \cite{lenstronomy}, and detector selection into a single reproducible pipeline. All lensing calculations are performed in the geometric optics regime and under the thin lens approximation, both of which are valid for galaxy-scale lenses in the LIGO frequency band (see \S\ref{sec:lensing_intro}).

The simulation is initialized with the following 
configuration, specifying the detector network, 
noise curves, and waveform approximant:

\begin{lstlisting}[style=numpyStyle]
ler = LeR(
    npool=6,
    event_type="BBH",
    ifos=['L1', 'H1'],
    psds={'L1': 'T2500310-v2_O5a_strain_psd.txt',
          'H1': 'T2500310-v2_O5a_strain_psd.txt'},
    waveform_approximant="IMRPhenomXPHM",
    spin_zero=False,
    spin_precession=True,
)
\end{lstlisting}

Here, \texttt{npool} sets the number of parallel worker 
processes used for the MC sampling and SNR 
calculations. The simulation was run in a Jupyter 
notebook on the CIT JupyterHub, using the \texttt{igwn} 
conda environment (Python 3.11.14) distributed via 
CVMFS, with 48 CPU cores available. The \texttt{event\_type="BBH"} argument selects the BBH source population model described in Table~\ref{tab:LER_params}, as opposed to the BNS or NS-BH options also supported by \textsc{LeR}. The \texttt{ifos} argument specifies the detector network over which the network SNR $\rho_\mathrm{net}$ is computed, here restricted to the two LIGO detectors, H1 and L1.

Here, \texttt{npool} sets the number of parallel worker 
processes used for the MC sampling and SNR 
calculations. The simulation was run in a Jupyter 
notebook on the CIT JupyterHub, using the \texttt{igwn} 
conda environment (Python 3.11.14) distributed via 
CVMFS, with 48 CPU cores available. The \texttt{event\_type="BBH"} argument selects the BBH source population model described in Table~\ref{tab:LER_params}, as opposed to the BNS or NS-BH options also supported by \textsc{LeR}. The \texttt{ifos} argument specifies the detector network over which the network SNR $\rho_\mathrm{net}$ is computed, here restricted to the two LIGO detectors, H1 and L1.

The \texttt{psds} argument sets the noise curve 
assigned to each detector, here the O5a design sensitivity PSD \cite{O5PSD}. This choice is motivated by the goal of simulating a realistic future observing scenario. The default PSD in \textsc{LeR} approximates O4 sensitivity, but no strongly lensed events were detected in O4. Simulating at O5a sensitivity instead, the first epoch of the O5 run with a BNS range of 225\,Mpc \cite{O5PSD}, provides a more informative and forward-looking setting for this study. Figure~\ref{fig:psd_O5a} shows the projected O5a strain 
sensitivity curve used in this work, compared against 
the L1 O4 sensitivity achieved in September 2024 and 
the later O5b and O5c projections.

\begin{figure}[h!]
    \centering
    \includegraphics[width=0.6\linewidth]
    {Figures/psd_O5a.png}
    \caption[Projected O5 strain sensitivity curves]{Projected strain sensitivity curves for the 
    O5 observing run (O5a, O5b, and O5c), compared against 
    the L1 O4 DARM sensitivity achieved in September 2024 
    (grey). The O5a curve (purple), adopted in this work, 
    corresponds to a binary neutron star range of 
    225\,Mpc. Dashed lines show the O5 thermal coating 
    noise and O5c quantum noise budgets. Source: 
    LIGO-T2500310-v2 \cite{O5PSD}.}
    \label{fig:psd_O5a}
\end{figure}

The \texttt{waveform\_approximant} argument selects 
\texttt{IMRPhenomXPHM} \cite{IMRPhenomXPHM} for the SNR 
computation, which incorporates spin precession and 
higher-order multipole moments. Finally, \texttt{spin\_zero=False} enables non-zero component spins in the source population, and \texttt{spin\_precession=True} allows for spin tilts misaligned with the orbital angular momentum, so that the resulting waveforms include precession effects. Both are necessary for the spin parameters listed in Table~\ref{tab:LER_params} to be sampled rather than fixed to zero.

\begin{table}[h!]
\centering
\renewcommand{\arraystretch}{1.2}
\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}p{6.5cm}p{8.5cm}}
\hline\hline\\[-11.5pt]
\multicolumn{1}{c}{Parameter} & \multicolumn{1}{c}{Distribution / value} \\[2.75pt]
\hline\\[-8.5pt]
\multicolumn{2}{l}{\textbf{Cosmology}} \\
\quad $H_0$, $\Omega_m$, $\Omega_\Lambda$ & 
$70\,\mathrm{km\,s^{-1}\,Mpc^{-1}}$, 0.3, 0.7 (flat $\Lambda$CDM) 
\cite{phurailatpam2026lerlvkligovirgokagracollaboration} \\[2pt]
\hline
\multicolumn{2}{l}{\textbf{BBH source parameters}} \\
\quad Component masses $m_1,\,m_2$ & 
Power Law + Gaussian (LIGO O3) \cite{Abbott2021pop} \\
\quad Spin magnitudes $a_1,\,a_2$ & 
Uniform $[0,\,1]$ \\
\quad Spin tilt angles $\theta_1,\,\theta_2$ & 
Sine prior on $[0,\,\pi]$ \\
\quad Azimuthal spin angles $\phi_{12},\,\phi_{JL}$ & 
Uniform $[0,\,2\pi]$ \\
\quad Inclination $\theta_{JN}$ & 
Sine prior on $[0,\,\pi]$ \\
\quad Polarization $\psi$ & 
Uniform $[0,\,\pi]$ \\
\quad Coalescence phase $\phi_c$ & 
Uniform $[0,\,2\pi]$ \\
\quad Sky location $(\alpha,\,\delta)$ & 
Isotropic \\
\quad Source redshift $z_s$ & 
Merger rate density \cite{Oguri2018} \\[2pt]
\hline
\multicolumn{2}{l}{\textbf{Lens galaxy parameters}} \\
\quad Optical depth $\tau(z_s)$ & 
$\left(D_c[\mathrm{Gpc}]/62.2\right)^3$ (SIS analytic) 
\cite{Haris2018} \\
\quad Velocity dispersion $\sigma_v$ & 
$161\cdot\mathrm{GenGamma}(0.869,\,2.67)\ \mathrm{km\,s^{-1}}$
\cite{Choi_2007} \\
\quad Lens redshift $z_L$ & 
Conditioned on $z_s$ (SDSS catalogue) \\
\quad Density slope $\gamma$ & 
$\mathcal{N}(2.0,\,0.2)$ 
\cite{Koopmans_2006,Auger_2010} \\
\quad Axis ratio $q$ & 
Rayleigh$(s(\sigma_v))$, $q\in[0.2,\,1]$ 
\cite{Wierda_2021} \\
\quad External shear $\gamma_1,\,\gamma_2$ & 
$\mathcal{N}(0,\,0.05^2)$ \cite{phurailatpam2026lerlvkligovirgokagracollaboration} \\
\quad Lens model & 
EPL + external shear \cite{Tessore2015} \\[2pt]
\hline
\multicolumn{2}{l}{\textbf{Detectability}} \\
\quad Network SNR threshold & 
$\rho_\mathrm{net} \geq 10$ \\
\quad Minimum detectable images & 
2 \\
\hline
\end{tabular*}
\caption{Source and lens population parameters used by \textsc{LeR} v0.4.2 \cite{phurailatpam2026lerlvkligovirgokagracollaboration} 
to generate the strongly lensed BBH population. All models 
correspond to its default configuration.}
\label{tab:LER_params}
\end{table}

The population is generated by drawing events in 
batches of 20\,000 until 500 strongly lensed 
detectable events are collected, each requiring 
that at least two images independently satisfy 
$\rho_\mathrm{net} \geq 10$:

\begin{lstlisting}[style=numpyStyle]
lensed_params = ler.selecting_n_lensed_detectable_events(
    size=500,
    batch_size=20000,
    snr_threshold=10,
    num_img=2,
    resume=False,
    output_jsonfile="n_lensed_params_bbh.txt",
)
\end{lstlisting}

The SNR threshold of $\rho_\mathrm{net} \geq 10$ and 
the requirement of at least two detectable images follow standard detection criteria adopted in GW searches, so that the resulting population reflects events that a real detector network could plausibly identify and confidently claim as lensed, rather than a theoretical population extending down to marginal or sub-threshold significance.

A total of approximately 152\,000 candidate events 
were drawn to collect the 500 detectable ones, 
yielding a total lensed event rate of 
$\mathcal{R}_L \approx 0.75\,\mathrm{yr}^{-1}$ 
for this detector configuration and sensitivity. 
This value is consistent with early forecasts for 
second-generation detector networks at design-like 
sensitivity, which span roughly $0.1$-$10\,\mathrm{yr}^{-1}$ 
depending on the assumed high-redshift merger rate and 
detector network \cite{Wierda_2021}, and is somewhat lower than forecasts for the full three- or four-detector network, reflecting the more limited H1+L1 configuration adopted here.


\section{Injected parameter distributions}
\label{sec:injected_params}

The 500 events produced by \textsc{LeR} represent the strongly lensed population detectable under ideal observing conditions. To obtain a more realistic injection set, a duty cycle filter is applied: for each image of each event, both H1 and L1 are independently activated with probability 
$p_\mathrm{det} = 0.70$, reflecting typical O4/O5 
single-detector duty cycles \cite{GWTC5}. An image is 
retained only if both detectors are simultaneously active and $\rho_\mathrm{net} \geq 10$. Events with fewer than two surviving images are discarded; when more than two images survive, only the two with the highest network SNR are kept. After applying this filter once to the fixed 500-event 
population, 157 events are selected as the final injection set analyzed with \textsc{bilby}.

The 500 events produced by \textsc{LeR} represent the strongly lensed population detectable under ideal observing conditions. To obtain a more realistic injection set, a duty cycle filter is applied: for each image of each event, both H1 and L1 are independently activated with probability 
$p_\mathrm{det} = 0.70$, reflecting typical O4/O5 
single-detector duty cycles \cite{GWTC5}. An image is 
retained only if both detectors are simultaneously active and $\rho_\mathrm{net} \geq 10$. Events with fewer than two surviving images are discarded; when more than two images survive, only the two with the highest network SNR are kept. 

After applying this filter once to the fixed 500 event 
population, 157 events are selected as the final injection set analyzed with \textsc{bilby}. Table~\ref{tab:selection_funnel} summarizes the successive stages of this selection process, from the initial MC draws performed by \textsc{LeR} to this final injection set.



\begin{table}[h!]
\centering
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{lcc}
\hline\hline
\multicolumn{1}{c}{Selection stage} & Number of events & Retained (\%) \\
\hline
Candidate events drawn by \textsc{LeR} & $\sim 152\,000$ & --- \\
Detectable lensed events ($\geq 2$ images with 
$\rho_\mathrm{net} \geq 10$) & 500 & $100$ \\
Duty cycle filter ($p_\mathrm{det} = 0.70$ per 
detector) & 309 & $61.8$ \\
Duty cycle $+$ SNR cut ($\geq 2$ images with 
$\rho_\mathrm{net} \geq 10$) & 157 & $31.4$ \\
\hline
\end{tabular}
\caption{Number of events surviving each stage of the selection process. Events drawn by \textsc{LeR} are first required to have at least two images with $\rho_\mathrm{net} \geq 10$ under ideal observing conditions, yielding the 500 detectable events used as the baseline for the percentages below. A duty cycle filter is then applied, independently 
activating each detector per image with probability 
$p_\mathrm{det}=0.70$ (309 events with at least two images surviving, regardless of SNR). The SNR threshold is finally re-applied, since some surviving images may fall below $\rho_\mathrm{net}=10$ once only active detectors are considered, leaving 157 events. All percentages are relative to the 500 detectable events.}
\label{tab:selection_funnel}
\end{table}

Figures~\ref{fig:param_dists_1} and \ref{fig:param_dists_2} show the distributions of the key parameters for the full detectable population ($N=500$, dashed) and the final 
injection set ($N=157$, solid). Figure~\ref{fig:param_dists_1} compares the optimal SNR distributions for L1, H1, and the network (left), and the primary source-frame mass $m_1^{\mathrm{src}}$ (right). The network SNR distribution for the filtered population is 
truncated at $\rho_\mathrm{net} = 10$ by construction, and its peak shifts to higher SNR relative to the full population, reflecting the removal of images that fall below the threshold once only the active detectors are considered; the individual L1 and H1 distributions can extend below 
$\rho = 10$ even after filtering, since the selection criterion applies to the network SNR rather than to each detector separately. The mass distribution reflects the underlying LIGO O3 Power Law + Gaussian population model adopted by \texttt{LER} v0.4.2; the duty cycle and SNR 
selection preferentially retains events with 
$m_1^{\mathrm{src}}$ in the $25$--$35\,M_\odot$ 
range, consistent with higher-mass systems producing intrinsically louder signals and thus being more robust to the loss of detector uptime, while the high-mass tail above $\sim 40\,M_\odot$ is largely absent from the filtered population.

\begin{figure}[h!]
    \centering
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]
        {Figures/snr_L1_H1_net_overlay_ler.png}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]
        {Figures/mass_1_source_hist_overlay_ler.png}
    \end{subfigure}
    \caption{Left: optimal SNR for L1 
    ($\rho_\mathrm{opt}^{\mathrm{L1}}$, light orange), 
    H1 ($\rho_\mathrm{opt}^{\mathrm{H1}}$, orange), 
    and the network ($\rho_\mathrm{opt}^{\mathrm{net}}$, 
    brown). Right: primary source-frame mass 
    $m_1^{\mathrm{src}}$.}
    \label{fig:param_dists_1}
\end{figure}

Figure~\ref{fig:param_dists_2} compares the true 
luminosity distance $d_L$ against the effective 
luminosity distance $d_L^{\mathrm{eff}}$ (left), 
and the image magnification $\mu$ (right). Because 
magnification boosts the observed signal amplitude, 
$d_L^{\mathrm{eff}} = d_L/\sqrt{|\mu|}$ is 
systematically smaller than the true distance, 
shifting the effective-distance distributions 
toward lower values relative to $d_L$. The 
magnification distribution, shown prior to the duty 
cycle filter for the full population and after it 
for the injection set, is strongly peaked near 
$\mu \sim 0$ with extended tails; negative values 
correspond to Type II images (saddle points of the 
Fermat potential), which have mirror-inverted 
parity and acquire a $\pi/2$ Morse phase shift (see 
\S\ref{sec:strong_lensing}). The horizontal axis of 
the magnification panel is clipped to the 
$0.5$--$99.5$ percentile range of the combined data 
to make the shape of the peak and tails visible, 
excluding a small number of extreme outliers 
reaching $|\mu| \sim 10^3$.

\begin{figure}[h!]
    \centering
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]
        {Figures/luminosity_distance_comparison_ler.png}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]
        {Figures/magnifications_hist_overlay_ler.png}
    \end{subfigure}
    \caption{Left: true luminosity distance $d_L$ 
    (brown) versus effective luminosity distance 
    $d_L^{\mathrm{eff}}$ (orange). Right: image 
    magnification $\mu$.}
    \label{fig:param_dists_2}
\end{figure}

Figure~\ref{fig:mass_scatter} shows the joint 
$m_1^{\mathrm{src}}$--$m_2^{\mathrm{src}}$ 
distribution for the 500 detectable events, and 
Figure~\ref{fig:timedelay_hist} shows the time 
delay distribution between the two lensed images 
for the same population.

\begin{figure}[h!]
    \centering
    \includegraphics[width=0.8\linewidth]
    {Figures/m1s_vs_m2s_ler.png}
    \caption{Source-frame component masses 
    $m_1^{\mathrm{src}}$ and $m_2^{\mathrm{src}}$ for 
    the 500 detectable lensed BBH events selected by 
    \textsc{LeR}, colour-coded by network SNR 
    $\rho_\mathrm{net}$. Diagonal dashed lines mark 
    constant total mass $M$ and dotted lines mark 
    constant mass ratio $q$. The population traces the 
    underlying LIGO O3 Power Law + Gaussian model 
    (\S\ref{sec:LER_population}), with most events 
    clustering near $q=1$ and the highest-SNR events 
    concentrated around $M\sim50$--$70\,M_\odot$.}
    \label{fig:mass_scatter}
\end{figure}

\begin{figure}[h!]
    \centering
    \includegraphics[width=0.77\linewidth]
    {Figures/time_delays_ler.png}
    \caption{Distribution of time delays $\Delta t$ 
    between the two lensed images for the 500 
    detectable lensed BBH events, shown on a 
    logarithmic scale. The distribution spans from 
    minutes to months, consistent with galaxy-scale 
    lenses in the geometric optics regime. The Earth's 
    rotation during this interval is the primary 
    mechanism enabling improved sky localization 
    through joint analysis (\S\ref{sec:joint_PE_skyloc}).}
    \label{fig:timedelay_hist}
\end{figure}



\chapter{Parameter estimation pipeline}
\label{Parameter_estimation_pipeline}

\section{Pipeline overview}
\label{sec:pipeline_overview}
% Diagrama de flujo: LeR -> generacion de .ini -> single PE (bilby_pipe x2)
% -> joint PE (hanabi_joint_pipe) -> outputs JSON -> post-proceso (skymaps)

\section{Single-image analysis with \textsc{bilby}}
\label{sec:bilby_config}
% Sampler dynesty (nlive=1000, naccept=60, acceptance-walk, n-parallel=3)
% Datos: duration=8s, fs=2048, f_min=20, PSD O5a
% Approximant IMRPhenomXPNR (y por que difiere del XPHM de LeR)
% Source model de Hanabi (strongly_lensed_BBH_waveform) -> ec. (lensed_strain)
% Priors: chirp mass, mass ratio, spins, sky location, distancia

\section{Joint analysis with \textsc{hanabi}}
\label{sec:hanabi_joint}
% hanabi_joint_pipe, n-triggers=2, trigger-ini-files (image1 + image2)
% common-parameters (intrinsecos + ra/dec compartidos)
% lensing-prior-dict: relative_magnification, image_type DiscreteUniform(N=3)
% Por que el joint mejora la localizacion (enlace al Cap. 3)

\section{Automation and infrastructure on Picasso}
\label{sec:automation_picasso}
% Generacion automatica de los .ini, SLURM, contenedores Singularity
% Scripts de monitorizacion y backup
% Los 7 eventos descartados por problemas de computo (157 -> 150)

\section{Parameter recovery and validation}
\label{sec:parameter_recovery}
% Violines single vs joint superpuestos (varios parametros, 150 eventos)
% Scatter m1-m2 y ratio q (recuperado vs inyectado)
% Residuals (inyectado - recuperado)



\chapter{Sky localization}
\label{Skyloc}

\section{From posterior samples to sky maps}
\label{sec:skymap_generation}
% ligo-skymap-from-samples sobre los posteriors de single y joint
% ligo-skymap-stats para extraer el area al 90%
% breve, parte metodologica

\section{Sky localization: single-image versus joint analysis}
\label{sec:skyloc_comparison}

  \subsection{90\% credible area}
  \label{sec:area_comparison}
  % CDF y PDF del area al 90% (single vs joint)
  % mediana single (571) vs joint (99.9), factor ~5, fraccion bajo 10 deg2
  % scatter area vs network SNR (img1, img2)

  \subsection{Constellation localization}
  \label{sec:constellation}
  % un unico plot: top constellation [%] vs network SNR
  % el joint concentra eventos cerca del 100%




To summarize, in this chapter we have...